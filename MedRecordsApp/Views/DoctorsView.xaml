<UserControl x:Class="MedRecordsApp.Views.DoctorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="2*"/>
    </Grid.RowDefinitions>

    <!-- Operations TabControl -->
    <TabControl Grid.Row="0" Margin="10">
      <!-- Add Tab -->
      <TabItem Header="Add">
        <Grid Margin="20">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <!-- Row 1 -->
          <StackPanel Grid.Row="0" Grid.Column="0" Margin="5">
            <TextBlock Text="Doctor Name:" HorizontalAlignment="Left" Margin="0,0,0,5"/>
            <TextBox x:Name="AddNameTextBox" Padding="5"/>
          </StackPanel>

          <StackPanel Grid.Row="0" Grid.Column="1" Margin="5">
            <TextBlock Text="Specialization:" HorizontalAlignment="Left" Margin="0,0,0,5"/>
            <TextBox x:Name="AddSpecializationTextBox" Padding="5"/>
          </StackPanel>

          <Button x:Name="AddSaveButton" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                            Content="Save" Width="120" Height="35" 
                            HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0"
                            Click="AddSaveButton_Click"/>
        </Grid>
      </TabItem>

      <!-- Update Tab -->
      <TabItem Header="Update">
        <Grid Margin="20">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <!-- Row 1 -->
          <StackPanel Grid.Row="0" Grid.Column="0" Margin="5">
            <TextBlock Text="Doctor ID:" HorizontalAlignment="Left" Margin="0,0,0,5"/>
            <ComboBox x:Name="UpdateDoctorIDComboBox" Padding="5"
                                  DisplayMemberPath="DoctorID" SelectedValuePath="DoctorID"/>
          </StackPanel>

          <StackPanel Grid.Row="0" Grid.Column="1" Margin="5">
            <TextBlock Text="Doctor Name:" HorizontalAlignment="Left" Margin="0,0,0,5"/>
            <TextBox x:Name="UpdateNameTextBox" Padding="5"/>
          </StackPanel>

          <!-- Row 2 -->
          <StackPanel Grid.Row="1" Grid.Column="0" Margin="5" Grid.ColumnSpan="2">
            <TextBlock Text="Specialization:" HorizontalAlignment="Left" Margin="0,0,0,5"/>
            <TextBox x:Name="UpdateSpecializationTextBox" Padding="5"/>
          </StackPanel>

          <Button x:Name="UpdateSaveButton" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" 
                            Content="Update" Width="120" Height="35" 
                            HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0"
                            Click="UpdateSaveButton_Click"/>
        </Grid>
      </TabItem>

      <!-- Delete Tab -->
      <TabItem Header="Delete">
        <Grid Margin="20">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <StackPanel Grid.Row="0" Grid.Column="0" Margin="5" Grid.ColumnSpan="2">
            <TextBlock Text="Doctor ID:" HorizontalAlignment="Left" Margin="0,0,0,5"/>
            <ComboBox x:Name="DeleteDoctorIDComboBox" Padding="5"
                                  DisplayMemberPath="DoctorID" SelectedValuePath="DoctorID"/>
          </StackPanel>

          <Button x:Name="DeleteButton" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                            Content="Delete" Width="120" Height="35" 
                            HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0"
                            Click="DeleteButton_Click"/>
        </Grid>
      </TabItem>

      <!-- Search Tab -->
      <TabItem Header="Search">
        <Grid Margin="20">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <StackPanel Grid.Row="0" Grid.Column="0" Margin="5" Grid.ColumnSpan="2">
            <TextBlock Text="Search Term:" HorizontalAlignment="Left" Margin="0,0,0,5"/>
            <TextBox x:Name="SearchTextBox" Padding="5"/>
          </StackPanel>

          <Button x:Name="SearchButton" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                            Content="Search" Width="120" Height="35" 
                            HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0"
                            Click="SearchButton_Click"/>
        </Grid>
      </TabItem>
    </TabControl>

    <!-- DataGrid -->
    <DataGrid x:Name="DoctorsDataGrid" Grid.Row="1" Margin="10"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  AlternatingRowBackground="#F9F9F9"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  SelectionChanged="DoctorsDataGrid_SelectionChanged">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Doctor ID" Binding="{Binding DoctorID}" Width="100"/>
        <DataGridTextColumn Header="Name" Binding="{Binding DoctorName}" Width="*"/>
        <DataGridTextColumn Header="Specialization" Binding="{Binding Specialization}" Width="200"/>
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>